# Deterministic Algorithms

Phase 4 starts with pure algorithm modules so correctness can be tested independently of Kafka and Neo4j:

- `correlation`: temporal, spatial, entity, relationship, and source-confidence scoring.
- `graph`: BFS impact traversal and weighted shortest path.
- `risk`: versioned weighted risk policy with transparent components.

